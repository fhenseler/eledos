import { ServerFilterPipe } from './server-filter.pipe';

describe('ServerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ServerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
